# ── Identity (Print Squirel) ──
CLIENT_SLUG=printsquirel
NEXT_PUBLIC_CLIENT_SLUG=printsquirel

# ── Portal (unique per Vercel project — never share across clients) ──
PORTAL_SECRET=

# ── WhatsApp ──
WHATSAPP_ENABLED=false
WHATSAPP_CLIENT_NAME=Print Squirel

# Optional / Dev Only
# WHATSAPP_TEST_MODE=true
# WHATSAPP_TEST_PHONE=

# Also copy all vars from .env.shared.example into this Vercel project
