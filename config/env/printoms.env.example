# ── Identity (Printoms) ──
CLIENT_SLUG=printoms
NEXT_PUBLIC_CLIENT_SLUG=printoms

# ── Portal (unique per Vercel project — never share across clients) ──
PORTAL_SECRET=

# ── WhatsApp (provider TBD — SmartPing) ──
WHATSAPP_ENABLED=false
WHATSAPP_CLIENT_NAME=Printoms

# Optional / Dev Only
# WHATSAPP_TEST_MODE=true
# WHATSAPP_TEST_PHONE=

# Also copy all vars from .env.shared.example into this Vercel project
